.container
  .table-responsive
    center(style="margin-top: 6rem; margin-bottom: 4rem;")    
      h1.display-4.col-auto #{data["mid"]}
      h5.mt-2.col-auto #{data["name"]} 
      br
      blockquote.blockquote.text-center
        p.mb-0 
          span            
            | A list of activities filtered by MID and orderd by timestamp. Activity actions applied to messages are grouped by services. Like multiple fields mapping in PDO or fields slected for rule evaluation. 
        footer.blockquote-footer
          | build for  
          cite(title='activities log')           
            button#showFlow.btn.btn-link Payment flow for MID #{data["mid"]}
      br
      .mt-2.p-2(style="background: #ecf0f1;")
        button#compareFlow.btn.btn-link.text-success.font-weight-bold
          | Compare payment with MID #{data["mid"]}
        span to any payment from the payments list
    br        
    table#activities.table.table-striped.table-bordered(style="width:100%;")
      thead
        tr
          th Time
          th Service 
          th Activity