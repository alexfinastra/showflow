.container(style="max-width: 125%")
	center(style="margin-top: 6rem;")
		h3.col-auto Compare payments Flows 
		span 
		| The similarity between flows is 87%
		br 
	#splitview(style="margin-top: 3rem;")
		#left.split    
			.row#payflow-left(style="margin: 5px;")
				.col-md-12.mt-5                
					center
						h4.col-auto Original Flow
						span.font-italic.text-secondary
						| with MID : #{data["mid"]}
						br 
					ul.timeline(style="margin-top: 1.5rem;")
						if data["items"] != null && data["items"] != undefined
							each item in data["items"]  
								include flow_item
		#right.split(style="margin-left: 5px; background-color: lightgrey; border: 1px solid grey;")
			.row#payflow-right(style="margin: 5px;")
				.col-md-12.mt-5                
					center
						h4.col-auto Another Flow
						span.font-italic.text-secondary
						| with MID : #{data["mid"]}
						br 
					ul.timeline(style="margin-top: 1.5rem;")
						if data["items"] != null && data["items"] != undefined
							each item in data["items"]  
								include flow_item